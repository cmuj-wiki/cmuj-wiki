<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512">
  <!-- Background -->
  <rect width="512" height="512" fill="#5c6bc0"/>

  <!-- Medical cross -->
  <g fill="#ffffff">
    <!-- Vertical bar -->
    <rect x="206" y="106" width="100" height="300" rx="10"/>
    <!-- Horizontal bar -->
    <rect x="156" y="156" width="200" height="100" rx="10"/>
  </g>

  <!-- Book spine effect (left side) -->
  <rect x="20" y="80" width="40" height="352" fill="#4a5ab8" rx="5"/>

  <!-- Small accent - medical symbol -->
  <circle cx="256" cy="420" r="25" fill="#ffffff" opacity="0.3"/>
  <text x="256" y="435" font-family="Arial, sans-serif" font-size="32" font-weight="bold" fill="#5c6bc0" text-anchor="middle">CM</text>
</svg>
